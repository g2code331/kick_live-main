# GENERATED by scripts/build-web-env.mjs from workers/wrangler.toml [env.staging.vars]. Do not edit — edit that file and re-run
# `npm run web:env`. `npm run web:env:check` fails CI if this drifts, so a Pages build can never again be the
# only place a project's identity is remembered.
#
# Selected by mode: `npm run build:web:staging` → vite --mode staging → this file. A shell-provided VITE_* still
# wins (Vite reads process.env first), which is how the CI workflow keeps overriding it from GitHub Environments.
# The anon key is public by design (RLS is the enforcement layer); a service_role key must NEVER appear here.
VITE_SUPABASE_URL=https://opvkvbabryuipzwcanrv.supabase.co
VITE_SUPABASE_ANON_KEY=sb_publishable_PywK8NqUvS4KfLv0TktpVg_LUaXXBm6
VITE_EXPECTED_PROJECT_REF=opvkvbabryuipzwcanrv
VITE_API_BASE_URL=https://kicklive-api-staging.g2code332.workers.dev
