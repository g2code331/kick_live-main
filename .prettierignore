# Generated / vendored / not ours to reformat in a release-pipeline PR.
node_modules
dist
renderer/dist
build
release
coverage
public
*.min.js
package-lock.json

# Pre-existing app code: it is not touched by this pipeline and reformatting it here would bury the
# release work in 40+ files of style churn. Formatting-only sweep is a separate PR (see
# docs/RELEASE-PIPELINE.md "Known gaps"). New/edited files under src are still listed explicitly.
src/pages/**
src/components/**
src/hooks/**
src/data/**
src/lib/**
src/types/**
src/App.tsx
src/index.css
.opencode/**
.vercel/**
DEPLOYMENT*.md
!src/components/UpdateControl.tsx
!src/lib/updates.ts
!src/lib/app-shell.ts
!src/lib/use-update-control.ts
# Phase 1 added these as new files, and Phase 3/4 added whole directories, so they are formatted from day
# one rather than inherited: a directory that did not exist before the pipeline has no churn to hide behind.
!src/lib/env.ts
!src/lib/log.ts
!src/lib/access.ts

# Generated by `node scripts/query-audit.mjs --write`; prettier would reformat the table widths the
# generator emits, and the file is only meaningful as that script writes it.
docs/data/phase4-query-inventory.md
!src/lib/live/**
!src/lib/data/**
!src/lib/api/**

# Both JSON files below are generated by their script (`--write-baseline` / `--write`) and are reviewed as
# diffs of those outputs. Prettier would wrap the long `note` strings the generators emit, which makes a
# faithful regeneration look like a hand edit — so the generator owns the formatting.
scripts/query-audit.baseline.json
scripts/bundle-budget.json
