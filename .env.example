# ─────────────────────────────────────────────────────────────────────────────
# Kick Live — environment template (Phase 1)
#
# Copy to `.env.local` for local dev. Nothing in this file is a secret *by
# Supabase's own threat model for browser apps*: the anon key is public and
# Postgres RLS is the enforcement layer. The moment you paste a `service_role`
# or `service_secret` key into a `VITE_*` variable you have published it — those
# keys belong to `workers/` (Cloudflare secrets) and to server-side scripts only.
#
# Both VITE_SUPABASE_* values must come from the SAME project: the client now
# refuses to boot if the URL's project ref and the key's `ref` claim disagree
# (this repo previously shipped a URL and a key from two different projects).
# ─────────────────────────────────────────────────────────────────────────────

# Supabase project URL, no trailing slash.
VITE_SUPABASE_URL=https://YOUR-PROJECT-REF.supabase.co

# Publishable ("anon") key: role claim = anon. Never the service_role key.
VITE_SUPABASE_ANON_KEY=eyJ...

# ── Phase 2: the Worker API layer (optional) ────────────────────────────────
# Origin of the API Worker, no trailing slash and no path. LEAVE THIS EMPTY unless you
# are deliberately pointing a build at a remote Worker: empty means same-origin /api, which
# the Vite dev server proxies to `wrangler dev` on 127.0.0.1:8787 and which Cloudflare serves
# directly when the app and the API share a zone. A dev build that names a non-local origin here
# is refused at boot unless VITE_API_ALLOW_REMOTE=1 — that guard is what keeps `npm run dev`
# from writing to production data.
VITE_API_BASE_URL=
VITE_API_ALLOW_REMOTE=

# Optional — desktop/PWA update feed (see docs/RELEASE-PIPELINE.md).
VITE_UPDATE_MANIFEST_URL=
VITE_UPDATE_CHANNEL=stable

# ── Push notifications (optional) ────────────────────────────────────────────
# The PUBLIC Firebase web config, needed only to let a browser obtain an FCM registration token. All
# of these are public by design, exactly like the anon key above: they identify the project to Google's
# SDK. The authority to actually SEND a push lives in the Worker's FCM_SERVICE_ACCOUNT secret (a
# Cloudflare secret, never a VITE_* var). Leave empty and push cleanly degrades to the in-app inbox.
# Values: Firebase console → Project settings → your web app; the VAPID key is under Cloud Messaging →
# Web Push certificates. In deployment these are set in workers/wrangler.toml [env.<env>.vars] and the
# generator (`npm run web:env`) passes them through into the mode file.
VITE_FIREBASE_API_KEY=
VITE_FIREBASE_AUTH_DOMAIN=
VITE_FIREBASE_PROJECT_ID=
VITE_FIREBASE_STORAGE_BUCKET=
VITE_FIREBASE_MESSAGING_SENDER_ID=
VITE_FIREBASE_APP_ID=
VITE_FIREBASE_VAPID_KEY=

# ── Server side only (never VITE_*, never in a browser bundle) ────────────────
# Used by `workers/` (Phase 2). Kept here as the documented list, not as values.
#
# SUPABASE_URL              same project, used by the Worker with a service-role key
# SUPABASE_PROJECT_REF      the ref this environment may talk to; the Worker refuses a mismatch
# SUPABASE_SERVICE_ROLE_KEY legacy JWT service role — Cloudflare secret, not a var above
# SUPABASE_JWT_SECRET       project JWT secret, to verify user tokens in the Worker
# R2 bucket                media storage is a *binding* (`MEDIA_BUCKET` in workers/wrangler.toml),
#                        not a key: there is no access key to store anywhere, and a media
#                        credential in any env file is a bug, not a configuration (Phase 6)
# TURNSTILE_SITE_KEY / TURNSTILE_SECRET_KEY   signup + write endpoints (Phase 2)
